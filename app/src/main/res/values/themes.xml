<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KGTools" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/title</item>
        <item name="colorPrimaryVariant">@color/title</item>
        <item name="colorOnPrimary">@color/title</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/title</item>
        <item name="colorSecondaryVariant">@color/title</item>
        <item name="colorOnSecondary">@color/title</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBar" parent="Theme.KGTools">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Launch" parent="Theme.KGTools">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="Theme.KGTools.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.KGTools.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>